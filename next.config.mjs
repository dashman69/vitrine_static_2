// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Ajouter ici les domaines d'images externes si besoin
    // ex: remotePatterns: [{ protocol: 'https', hostname: 'images.unsplash.com' }]
    remotePatterns: [],
  },
};

export default nextConfig;
